/**
 * Modelo que representa un proveedor en el sistema de inventario (frontend).
 * Basado en la entidad ProveedorEntity del backend.
 */
export class Proveedor {
  idProveedor!: number;
  nombreProveedor!: string;
  telefono!: string;
  correo!: string;
}
